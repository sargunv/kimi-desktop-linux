{
  "name": "kimi-desktop-linux",
  "version": "0.1.0",
  "private": true,
  "description": "Repackage the latest Kimi Work desktop release for Linux",
  "type": "module",
  "packageManager": "pnpm@11.15.1",
  "engines": {
    "node": "24.15.0"
  },
  "scripts": {
    "build": "bash scripts/build.sh",
    "check": "dprint check && actionlint && node --check scripts/*.mjs && bash -n scripts/*.sh packaging/appimage/AppRun",
    "fmt": "dprint fmt",
    "package": "bash scripts/package-appimage.sh",
    "test": "pnpm check && node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@electron/asar": "4.2.0",
    "@napi-rs/cli": "3.7.0"
  }
}
